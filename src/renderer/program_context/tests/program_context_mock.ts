import { createMockInstance } from '/src/util/tests';
import { ProgramContext } from '../program_context';

export function createProgramContextMock() {
  return createMockInstance(ProgramContext);
}
